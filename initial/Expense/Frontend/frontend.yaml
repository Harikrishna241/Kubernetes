apiVerssion: v1
kind: pod 
metadata:
  name: expense
  labels:
    module: db
    prject: expense
    environmennt: Dev
spec:
  replicas: 3
  selector:
    matchLabels:
    module: db
    prject: expense
    environmennt: Dev
  template:
    metadata:
      labels:
        module: db
        prject: expense
        environmennt: Dev
    spec:
      containers:
      - name: mysql
        image: mysqlv1.0.0
        ports:
        - containerPort: 3306